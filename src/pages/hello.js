import React from "react"
import Layout from "@theme/Layout"
// js的页面是基于react的，编写一个react组件即可成为页面

export default () => {
    return (
        <Layout>
            <div>
                <h1>这是我的第一个页面</h1>
            </div>
        </Layout>
    )
}
